# Environment variables for the Triage Agent system
# Copy this file to .env and fill in the appropriate values

# OpenAI API key for LLM-based classification and response drafting
OPENAI_API_KEY=your_openai_api_key_here

# Frontend API base URL (optional, defaults to http://localhost:8000)
REACT_APP_API_BASE_URL=http://localhost:8000

# Gmail API credentials (not used in mock implementation)
# GOOGLE_CREDENTIALS=/path/to/credentials.json

# Twilio API credentials (not used in mock implementation)
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token
